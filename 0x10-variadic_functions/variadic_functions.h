#ifndef VARIADIC_H_
#define VARIADIC_H_

int_putchar(char c);
int sum _them_all(const unsingned int n, ...);
void print_number(const char *separator,const unsigned int n,...);
void print_strings(const char *separator, const unsigned int n, ...);
void print_all(const char *const format, ...);

#endif
