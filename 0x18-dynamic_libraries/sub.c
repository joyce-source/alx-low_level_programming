#include <stdio.h>
/**
  *int add - function that adds two integers
  *Return - value
  *void greet - function that prints a greeting message for the given name
  *Retun -Always (0)
  */
int add(int a, int b)
{
	return (a + b);
}

void greet(char *name)
{
	printf("Hello, %s!\n", name);
}
