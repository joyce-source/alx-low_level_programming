#include <stdio.h>
/**
 * main-entry point
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int ga;

	ga = 0;
	while
		(ga < 10) {
			putchar(ga + '0');
			ga++;
		}
	putchar('\n');
	return (0);
}
